[functions.admin-delete-user]
# The platform and @supabase/server both validate the signed-in user's JWT.
verify_jwt = true

[functions.admin-reset-password]
# The platform and @supabase/server both validate the signed-in user's JWT.
verify_jwt = true
